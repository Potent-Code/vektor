#ifndef SDL_H
#define SDL_H

#include "config.h"

/*#if HAVE_OPENGL_GL_H
	#include <OpenGL/gl.h>
#else
	#if HAVE_GL_GL_H
		#include <GL/gl.h>
	#endif
#endif

#if HAVE_OPENGL_GLU_H
	#include <OpenGL/glu.h>
#else
	#if HAVE_GL_GLU_H
		#include <GL/glu.h>
	#endif
#endif*/

#include <SDL/SDL.h>
#include <SDL/SDL_opengl.h>

#endif
